type: object
required:
  - project
  - storage
properties:
  project:
    type: object
  storage:
    type: object
  execution:
    type: object
    properties:
      hitl:
        type: object
        properties:
          mode:
            type: string
            enum: [minimal, selective, interactive]
  security:
    type: object
