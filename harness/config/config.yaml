project:
  name: developer-harness
  specification_dir: .
  workspace_dir: .

storage:
  database: state/harness.sqlite

execution:
  dry_run: true
  max_retries: 2

security:
  require_approval_for:
    - shell_command
    - file_delete
    - network_access
